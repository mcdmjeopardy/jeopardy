import React from 'react';
import styles from './Button.module.css';

const Button = () => {
  return (
    <div className={`${styles.container} ${styles.active} cont`}>
      Button
    </div>
  );
};

export default Button;
